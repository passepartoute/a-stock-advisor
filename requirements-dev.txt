# ============================================================
# A股多因子选股系统 - 开发依赖
# ============================================================

# ---------- 运行时依赖（生产环境必需）----------
akshare>=1.15.0
tushare>=1.4.0
pandas>=2.0.0
numpy>=1.24.0
pyyaml>=6.0
rich>=13.0.0

# ---------- 测试依赖 ----------
pytest>=7.0.0
pytest-cov>=4.0.0          # pytest 覆盖率插件

# ---------- 代码质量 ----------
black>=23.0.0              # 代码格式化
ruff>=0.1.0                # 快速 Python linter（替代 flake8 + isort）
mypy>=1.5.0                # 静态类型检查

# ---------- Jupyter / 交互式分析（可选）----------
jupyter>=1.0.0
matplotlib>=3.7.0
seaborn>=0.12.0

# ---------- 文档生成（可选）----------
mkdocs>=1.5.0
mkdocs-material>=9.0.0
